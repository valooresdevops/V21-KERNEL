import { ConditionalIncrementPipe } from './conditional-increment.pipe';

describe('ConditionalIncrementPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionalIncrementPipe();
    expect(pipe).toBeTruthy();
  });
});
